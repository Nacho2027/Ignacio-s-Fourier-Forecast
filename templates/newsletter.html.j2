<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <!-- Prevent dark mode inversion -->
    <meta name="color-scheme" content="only light">
    <meta name="supported-color-schemes" content="light">
    <title>{{ subject }}</title>
    <!--[if mso]>
    <noscript>
        <xml>
            <o:OfficeDocumentSettings>
                <o:PixelsPerInch>96</o:PixelsPerInch>
            </o:OfficeDocumentSettings>
        </xml>
    </noscript>
    <![endif]-->
    
    <!-- Google Fonts for Merriweather (Serif) -->
    <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@300;400;700&display=swap" rel="stylesheet">
    
    <style>
        /* Enforce light mode only - prevent dark mode inversion */
        :root {
            color-scheme: only light;
            supported-color-schemes: light;
        }

        /* Reset and Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.75;
            color: #1A1A1A;
            background-color: #FFFFFF;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            margin: 0;
            padding: 0;
        }
        
        /* Container - Wider for better reading */
        .email-container {
            max-width: 680px;
            margin: 0 auto;
            background-color: #FFFFFF;
        }
        
        /* Header - Simplified Aeon Style */
        .header {
            padding: 40px 20px 32px;
            text-align: center;
            border-bottom: 1px solid #E0E0E0;
            background-color: #FFFFFF;
        }
        
        /* Logo removed - doesn't fit properly */
        
        .newsletter-title {
            font-family: 'Merriweather', Georgia, 'Times New Roman', serif;
            font-size: 28px;
            font-weight: 400;
            color: #1A1A1A;
            letter-spacing: -0.5px;
            margin: 16px 0 8px;
        }
        
        .date {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            font-size: 13px;
            color: #6b7c8b; /* Dusty blue */
            font-weight: 400;
            letter-spacing: 0.5px;
            text-transform: uppercase;
        }
        
        /* Content Wrapper */
        .content {
            padding: 32px 20px;
            background-color: #FFFFFF;
        }
        
        /* Greeting - Clean Typography */
        .greeting {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            font-size: 15px;
            color: #1A1A1A;
            line-height: 1.75;
            margin-bottom: 32px;
            padding-bottom: 32px;
            border-bottom: 1px solid #E0E0E0;
        }
        
        /* Golden Thread - Elegant Quote Style */
        .golden-thread {
            padding: 32px 24px;
            margin-bottom: 48px;
            background-color: #fefbec; /* Warm cream background */
            border-top: 2px solid #9d1d20; /* Burgundy accent */
            border-bottom: 1px solid #E0E0E0;
            border-radius: 4px;
        }
        
        .golden-thread-label {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            font-size: 11px;
            font-weight: 600;
            color: #999999;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 16px;
        }
        
        .golden-thread-text {
            font-family: 'Merriweather', Georgia, serif;
            font-size: 18px;
            font-style: italic;
            font-weight: 300;
            color: #1A1A1A;
            line-height: 1.6;
        }
        
        /* Section - Aeon-style Headers */
        .section {
            margin-bottom: 64px;
        }
        
        .section-title {
            font-family: 'Merriweather', Georgia, serif;
            font-size: 24px;
            font-weight: 400;
            color: #1A1A1A;
            margin-bottom: 8px;
            padding-bottom: 16px;
            border-bottom: 2px solid #9d1d20; /* Burgundy accent */
            letter-spacing: -0.5px;
        }
        
        /* Section Preface - Subtle Introduction */
        .section-preface {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            font-size: 14px;
            font-style: italic;
            color: #333333; /* Dark gray for better readability */
            line-height: 1.75;
            margin-bottom: 24px;
            padding: 16px 0;
        }
        
        /* Article Item - Clean Card Design */
        .item {
            padding: 24px 0;
            border-bottom: 1px solid #E0E0E0;
            transition: transform 0.2s ease;
        }
        
        .item:last-child {
            border-bottom: none;
        }
        
        .item:hover {
            transform: translateX(2px);
        }
        
        .item-headline {
            font-family: 'Merriweather', Georgia, serif;
            font-size: 18px;
            font-weight: 700;
            color: #1A1A1A;
            margin-bottom: 12px;
            line-height: 1.4;
        }
        
        .item-headline a {
            color: #1A1A1A;
            text-decoration: none;
        }
        
        .item-headline a:hover {
            text-decoration: underline;
        }
        
        .item-content {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            font-size: 15px;
            color: #444444;
            line-height: 1.75;
            margin-bottom: 12px;
        }
        
        .item-meta {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            font-size: 13px;
            color: #999999;
            /* Email-compatible layout without flexbox */
        }

        .item-meta span {
            display: inline-block;
            vertical-align: middle;
            margin-right: 12px;
        }

        .item-meta span:last-child {
            margin-right: 0;
        }
        
        .item-source {
            font-weight: 500;
            color: #666666;
        }
        
        .item-time {
            color: #c67e61; /* Terracotta accent */
        }
        
        .item-meta-separator {
            color: #CCCCCC;
        }
        
        /* Delightful Surprise - Centered Quote */
        .surprise {
            padding: 48px 20px;
            margin: 48px 0;
            text-align: center;
            background-color: #fefbec; /* Warm cream background */
            border-top: 1px solid #c67e61; /* Terracotta border */
            border-bottom: 1px solid #c67e61;
            border-radius: 4px;
        }
        
        .surprise-label {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            font-size: 11px;
            font-weight: 600;
            color: #999999;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 20px;
        }
        
        .surprise-content {
            font-family: 'Merriweather', Georgia, serif;
            font-size: 17px;
            font-style: italic;
            font-weight: 300;
            color: #1A1A1A;
            line-height: 1.6;
            margin-bottom: 16px;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
        }
        
        .surprise-attribution {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            font-size: 14px;
            color: #666666;
            font-style: normal;
        }
        
        /* Footer - Minimal */
        .footer {
            border-top: 2px solid #9d1d20; /* Burgundy accent */
            padding: 32px 20px;
            text-align: center;
            background-color: #FFFFFF;
        }
        
        .footer-stats {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            font-size: 12px;
            color: #999999;
            margin-bottom: 16px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .footer-tagline {
            font-family: 'Merriweather', Georgia, serif;
            font-size: 14px;
            color: #666666;
            font-style: italic;
        }
        
        /* Links */
        a {
            color: #1A1A1A;
            text-decoration: underline;
            text-decoration-color: #CCCCCC;
            text-underline-offset: 2px;
            transition: all 0.2s ease;
        }
        
        a:hover {
            color: #9d1d20; /* Burgundy on hover */
            text-decoration-color: #9d1d20;
        }
        
        /* Mobile Responsive - Mobile First */
        @media only screen and (min-width: 600px) {
            /* Desktop Enhancements */
            .header {
                padding: 48px 40px 40px;
            }
            
            /* Logo removed */
            
            .newsletter-title {
                font-size: 36px;
            }
            
            .content {
                padding: 40px;
            }
            
            .section-title {
                font-size: 28px;
            }
            
            .item-headline {
                font-size: 20px;
            }
            
            .golden-thread-text {
                font-size: 20px;
            }
            
            .surprise-content {
                font-size: 19px;
            }
        }
        
        /* Mobile Specific Optimizations */
        @media only screen and (max-width: 599px) {
            .email-container {
                width: 100% !important;
            }
            
            /* Ensure readable font sizes on mobile */
            .item-content {
                font-size: 14px;
            }
            
            .item-meta span {
                margin-right: 8px;
                margin-bottom: 4px;
            }
            
            /* Better touch targets */
            a {
                padding: 4px 0;
                display: inline-block;
            }
            
            /* Optimize spacing for mobile */
            .section {
                margin-bottom: 48px;
            }
            
            .item {
                padding: 20px 0;
            }
        }

        /* Email Client Specific Fixes handled via conditional comments below */
    </style>

    <!--[if mso]>
    <style type="text/css">
        .email-container {
            width: 680px !important;
        }
        table {
            border-collapse: collapse;
        }
    </style>
    <![endif]-->
</head>
<body>
    <div class="email-container">
        <!-- Header -->
        <div class="header">
            <!-- Logo removed - doesn't fit properly -->
            <h1 class="newsletter-title">Ignacio's Fourier Forecast</h1>
            <div class="date">{{ date }}</div>
        </div>
        
        <!-- Main Content -->
        <div class="content">
            <!-- Greeting -->
            {% if greeting %}
            <div class="greeting">
                {{ greeting }}
            </div>
            {% endif %}
            
            <!-- Golden Thread -->
            {% if golden_thread %}
            <div class="golden-thread">
                <div class="golden-thread-label">Golden Thread</div>
                <div class="golden-thread-text">
                    {{ golden_thread }}
                </div>
            </div>
            {% endif %}
            
            <!-- Sections -->
            {% for section_key, section_data in sections.items() %}
            {% if section_data['items'] %}
            <div class="section">
                <h2 class="section-title">{{ section_key.replace('_', ' ').title() }}</h2>
                
                {% if section_data['intro_text'] %}
                <div class="section-preface">
                    {{ section_data['intro_text'] }}
                </div>
                {% endif %}
                
                {% for item in section_data['items'] %}
                <div class="item">
                    <h3 class="item-headline">
                        {% if item.source_url %}
                        <a href="{{ item.source_url }}">{{ item.headline }}</a>
                        {% else %}
                        {{ item.headline }}
                        {% endif %}
                    </h3>
                    <div class="item-content">
                        {{ item.summary }}
                    </div>
                    <div class="item-meta">
                        <span class="item-source">{{ item.source }}</span>
                        {% if item.time %}
                        <span class="item-meta-separator">·</span>
                        <span class="item-time">{{ item.time }}</span>
                        {% endif %}
                    </div>
                </div>
                {% endfor %}
            </div>
            {% endif %}
            {% endfor %}
            
            <!-- Delightful Surprise -->
            {% if delightful_surprise %}
            <div class="surprise">
                <div class="surprise-label">Today's Reflection</div>
                <div class="surprise-content">
                    {{ delightful_surprise.content }}
                </div>
                {% if delightful_surprise.attribution %}
                <div class="surprise-attribution">
                    — {{ delightful_surprise.attribution }}
                </div>
                {% endif %}
            </div>
            {% endif %}
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <div class="footer-stats">
                {{ total_items }} Stories · {{ estimated_read_time }} Min Read
            </div>
            <div class="footer-tagline">
                Signal from noise, distilled daily.
            </div>
        </div>
    </div>
</body>
</html>